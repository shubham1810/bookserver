
ISBN (GET)
    -Return
        --Title (got from scraper)
        --Author ()
        --URL of image (got from scraper)
        --category ()
        --summary ()
        --Ratings (got from scraper)

Create Database on Parse:
    --Add data of the books to parse backend
    --Only when receiving a get request form the app.